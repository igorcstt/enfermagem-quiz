using System.Collections;
using System.Collections.Generic;
using UnityEngine;
using System;
public class Enigma : MonoBehaviour
{
    String ctPergunta;
    String ctResposta;

   private int qtChances;
    private int qtPontos;

    public int QtPontos { get => qtPontos; set => qtPontos = value; }
    public int QtChances { get => qtChances; set => qtChances = value; }

    // Start is called before the first frame update
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
